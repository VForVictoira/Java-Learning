package Example4_13;

public class Student {
	double computeArea(Circle c) {
		double area = c.getArea();
		return area;
	}
	double computeArea(Tixing t) {
		double area = t.getArea();
		return area;
	}
}
